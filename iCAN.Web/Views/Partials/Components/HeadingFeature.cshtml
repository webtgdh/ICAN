@using iCAN.Core.Models;
@inherits UmbracoViewPage<HeadingViewModel>
@{
    if (string.IsNullOrWhiteSpace(Model.Headline))
    {
        return;
    }
}

<header class="c-heading-feature @Model.ModifierClass">
	@switch(Model.Level) {
		case "h1":
			<h1 class="c-heading-feature__headline u-mega" itemprop="headline">@Model.Headline</h1>
			break;
		case "h3":
			<h3 class="c-heading-feature__headline u-alpha" itemprop="headline">@Model.Headline</h3>
			break;
		default:
			<h2 class="c-heading-feature__headline u-alpha" itemprop="headline">@Model.Headline</h2>
			break;
	}
	@if (!String.IsNullOrWhiteSpace(Model.Subtitle)) {
		<div class="c-heading-feature__subtitle">
			<p>@Model.Subtitle</p>
		</div>
	}
</header>
